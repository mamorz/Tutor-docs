String[] array = {"Programmieren", "bereitet", "Freude"};

// for-loop
for (int i = 0; i < array.length; i++) {
   System.out.println(array[i]);
}

// foreach-loop
for (String i : array) {
   System.out.println(i);
}
