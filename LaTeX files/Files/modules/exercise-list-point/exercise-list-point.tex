\section{Aufgabe: Liste}

\begin{frame}{Aufgabe: Liste}
    \begin{block}{Liste von Punkten}
        Implementieren Sie die Klasse LinkedPointList, die Punkte (Klasse Point) mittels einer Liste verwaltet. Schreiben Sie hierzu die für Listen üblichen Schnittstellen: \texttt{LinkedPointListInterface.java}. Verwenden Sie hierzu die gegebene Klasse \texttt{Point.java}.
    \end{block}
    \begin{block}{einige Schnittstellenmethoden (nicht vollständig)}
        \begin{itemize}
            \item \lstinline{public void add(Point p);}
            \item \lstinline{public boolean contains(Point p);}
            \item \lstinline{public Point get(int index);}
            \item \lstinline{public Point getFirst()}
            \item \lstinline{public Point getLast();}
            \item \lstinline{public int indexOf(Point p);}
            \item \lstinline{public void remove(int index);}
        \end{itemize}
    \end{block}
\end{frame}
